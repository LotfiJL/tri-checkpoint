ALGORITHM SORT
VAR

    n = length of arr
    key, j;
    
BEGIN

    for i = 1 to n-1 ;
        key = arr[i] ;
        j = i - 1 ;
        while j >= 0 and arr[j] > key ;
            arr[j + 1] = arr[j];
            j = j - 1;
        arr[j + 1] = key;
        END while
    END FOR
END